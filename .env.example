# 复制为 .env 后填入（勿提交 .env）
# 同步到 Vercel: ./scripts/sync-vercel-anthropic.sh
#
# 必须是 Anthropic 控制台申请的 key（通常以 sk-ant- 开头），不要用 OpenAI 的 key。
# Vercel 控制台添加时：Environment 要勾选 Production（以及需要时的 Preview），
# 值不要加引号；保存后需 Redeploy 或重新 deploy 才生效。
# 部署后访问 /api/health 应看到 "anthropic_api_key_configured": true
ANTHROPIC_API_KEY=
